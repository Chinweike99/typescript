import React from 'react';

function App() {
  const handleClick = (count: number) => {
    console.log(`Count: ${count}`);
  };

  return (
    <div>
      <h1>Your fun time ...</h1>
    </div>
  );
}

export default App;